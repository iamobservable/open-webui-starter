# Kibana Configuration for ERNI-KI
# Минимальная конфигурация без базового пути

server.host: "0.0.0.0"
server.port: 5601

# Elasticsearch connection
elasticsearch.hosts: ["http://elasticsearch:9200"]

# Отключаем перезапись базового пути
server.rewriteBasePath: false
