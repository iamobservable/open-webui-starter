# GPU layout overrides (copy to .env and adjust per host).
# Рекомендуемая схема: GPU0 → Ollama (LLM), GPU1 → OpenWebUI/Docling (OCR/RAG).

# Ollama (primary LLM runtime)
OLLAMA_GPU_VISIBLE_DEVICES=0
OLLAMA_CUDA_VISIBLE_DEVICES=0
OLLAMA_GPU_DEVICE_IDS=0

# OpenWebUI (UI plugins, image captioning)
OPENWEBUI_GPU_VISIBLE_DEVICES=1
OPENWEBUI_CUDA_VISIBLE_DEVICES=1
OPENWEBUI_GPU_DEVICE_IDS=1

# Docling (OCR/ingestion). Если у вас один GPU, укажите MIG slice или ту же карту,
# но следите за приоритетами задач.
DOCLING_GPU_VISIBLE_DEVICES=1
DOCLING_CUDA_VISIBLE_DEVICES=1
DOCLING_GPU_DEVICE_IDS=1
